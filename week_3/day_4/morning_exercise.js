function evensOnly() {
    // logic in code block
    var limit = prompt("Enter a number to count up to");
    var counter = 0;
    while(counter <= limit) {
      if (counter % 2 == 0) {
        console.log(counter)
      }
      counter++; 
    }
  }
  
  evensOnly();

function count up to three evensOnly() {

    var limit = prompt()
}

// comments